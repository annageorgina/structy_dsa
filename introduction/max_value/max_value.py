def max_value(nums):
  temp_val = nums[0]
  for i in nums:
    if i > temp_val:
      temp_val = i
  return temp_val






def max_value(lis):
  val = lis[0]
  for ele in lis:
    if ele > val:
      val = ele
  return val
  



























































